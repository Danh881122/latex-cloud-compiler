services:
  - type: web
    name: latex-cloud-compiler
    env: python
    buildCommand: |
      apt-get update
      apt-get install -y texlive texlive-latex-extra texlive-fonts-recommended texlive-lang-other texlive-lang-cjk
      pip install -r requirements.txt
    startCommand: python3 server.py
